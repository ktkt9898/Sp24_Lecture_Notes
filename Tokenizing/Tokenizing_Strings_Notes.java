/*
 * Take a larger string a break it into smaller pieces
 * To tokenize, use a scanner delimiter (character that breaks apart)
 * A scanner's default delimiter is white space
 * the useDelimiter() method will break at a specific character
 * 
 * final String DELIMITERS = ",";
 * 
 * while(fileScan.hasNextLine()) {
 *  String line = fileScan.nextLine();
 *  Scanner lineScan = new Scanner(line)
 * }
 */