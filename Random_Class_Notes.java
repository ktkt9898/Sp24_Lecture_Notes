/**
 * Random class is a psuedo-random number generator
 * The seed value is the specific starting point
 * Only seed once when Random class is constructed
 * nextInt(int bound), if 6 is passed, only 0-5 is generated
 * Must be imported
 * Random generator = new Random();
 * int anyNum = generator.nextInt(); random number
 * int flip = generator.nextInt(2); random number between 0-1
 * x = generator.nextInt(width - circleDiameter);
 */