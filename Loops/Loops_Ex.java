package Loops;
import java.util.Scanner;

public class Loops_Ex {
    public static void main(String[] args){
        // Scanner input = new Scanner(System.in);

        // System.out.print("Enter a non negative number ");
        // int initialValue = 0;

        // while (initialValue <= 0) {
        //     System.out.print("Enter a non negative number ");
        //     initialValue = input.nextInt();
        //     System.out.println(initialValue);
        // }
        // System.out.println("Loop finished");

        // input.close();

        // int numTotal = 0;
        // int numCount = 0;
        
        // while (numCount < 3) {
        //     System.out.println("Enter number");
        //     int num = input.nextInt();
        //     numTotal = numTotal + num;
        //     numCount++;
        // }
        // System.out.println(numTotal);
        // input.close();

        // // user an interator over a string
        // Scanner strScan = new Scanner("The quick brown fox");

        // // count number of words
        // int wordsCount = 0;
        // // while we have characters that have not been seen,
        // // or else returns false
        // while (strScan.hasNext()){
        //     String nextWord = strScan.next();
        //     wordsCount++;
        // }
        // System.out.println("There were " + wordsCount + " words");
        // strScan.close();

        // WIP
//         int baseNum = 12;
//         int innerCount = 0;

//         while (baseNum <= 14) {
//             System.out.println(baseNum + ":");
//             while (innerCount <= 12) {
//                 System.out.print(" " + innerCount * 12);
//             }
//             innerCount = 0;
//             baseNum++;
//         }
//     }
// }

